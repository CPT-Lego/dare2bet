






 <% if current_user == @bet.user %>
          <%= link_to "Delete Bet", bet_path(@bet), class: "m-3 btn btn-dark m-2", data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
          <% end %>
          <hr>
