
 <%= render "shared/navbar" %>


<h1 class="custom-bg-info text-white p-3 mx-auto mt-3">Top Bets</h1>


<div class="bets-container pb-5">
  <% @bets.each do |bet| %>
    <div class="bet-card">
      <%= render "bet_card", bet: bet %>
    </div>
    <div class="card-space"></div>
  <% end %>
</div>

<footer class="footer">
  <%= render "shared/footer" %>
</footer>
